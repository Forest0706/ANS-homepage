# 📋 ANS 官网完整配置指南

## 🎯 配置概览

你需要完成以下4个配置步骤：

1. ✅ **Vercel 连接 Git** - 已创建项目，需要连接 Git 仓库
2. ✅ **配置域名 ans-scm.com** - 主域名配置
3. ✅ **配置子域名** - admin.ans-scm.com 和 wms.ans-scm.com
4. ✅ **更新 Logo** - 从 Supabase 获取并配置 logo

---

## 1️⃣ Vercel 连接 Git 仓库

### 步骤：

1. 访问 Vercel 项目：https://vercel.com/forestlis-projects/ans_homepage
2. 进入 **Settings** → **Git**
3. 点击 **Connect Git Repository**
4. 选择 **GitHub** 并授权
5. 选择仓库：`Forest0706/ANS-homepage`
6. 点击 **Import**

**完成后**：每次 `git push` 到 GitHub，Vercel 会自动部署。

---

## 2️⃣ 配置主域名 ans-scm.com

### 步骤：

1. **在 Vercel 中添加域名**
   - 项目 **Settings** → **Domains**
   - 点击 **Add Domain**
   - 输入：`ans-scm.com`
   - 点击 **Add**

2. **配置 DNS 记录**

   在你的域名注册商（如 Namecheap, Cloudflare, GoDaddy）配置：

   **推荐：CNAME 记录**
   ```
   类型: CNAME
   名称: @（或留空，表示根域名）
   值: cname.vercel-dns.com
   TTL: 3600（或自动）
   ```

   **或者：A 记录**
   ```
   类型: A
   名称: @
   值: 76.76.21.21
   TTL: 3600
   ```

   ⚠️ **注意**：Vercel 会显示具体的 DNS 值，请按照 Vercel 控制台中显示的配置。

3. **等待 DNS 生效**
   - 通常 5-60 分钟
   - 可以检查：`nslookup ans-scm.com`

4. **验证 SSL 证书**
   - Vercel 会自动为域名配置 SSL 证书
   - 访问 `https://ans-scm.com` 验证 HTTPS

---

## 3️⃣ 配置子域名（台账系统和 WMS 系统）

### 方案 A：两个独立的 Vercel 项目（推荐）

如果你的台账系统和 WMS 系统是独立部署的：

#### 台账系统 (admin.ans-scm.com)

1. **创建新 Vercel 项目**（或使用现有项目）
   - 项目名：`ans-admin`
   - 连接 Git 仓库（可以是同一个或不同的仓库）

2. **添加子域名**
   - 项目 **Settings** → **Domains**
   - 添加：`admin.ans-scm.com`

3. **配置 DNS**
   ```
   类型: CNAME
   名称: admin
   值: cname.vercel-dns.com
   ```

#### WMS 系统 (wms.ans-scm.com)

1. **创建新 Vercel 项目**
   - 项目名：`ans-wms`
   - 连接 Git 仓库

2. **添加子域名**
   - 添加：`wms.ans-scm.com`

3. **配置 DNS**
   ```
   类型: CNAME
   名称: wms
   值: cname.vercel-dns.com
   ```

### 方案 B：使用 Vercel 的路径路由（单项目）

如果你的台账和 WMS 系统在同一个项目中，需要：

1. 确保项目支持路径路由
2. 在 `vercel.json` 中配置 rewrites（已包含在项目中）

---

## 4️⃣ 配置 Logo（从 Supabase）

### 步骤：

1. **获取 Supabase Logo URL**

   - 登录 Supabase Dashboard
   - 进入 **Storage** → **Buckets**
   - 找到包含 logo 的 bucket（通常是 `public`）
   - 点击 logo 文件，复制 **Public URL**
   - URL 格式：`https://你的项目ID.supabase.co/storage/v1/object/public/bucket名称/logo文件名`

2. **配置环境变量**

   更新 `.env.local` 文件（如果不存在，从 `.env.example` 复制）：

   ```bash
   # 复制示例文件
   cp .env.example .env.local
   ```

   编辑 `.env.local`，添加：

   ```env
   # Logo URL（从 Supabase Storage 获取）
   VITE_LOGO_URL=https://你的项目ID.supabase.co/storage/v1/object/public/public/logo.png
   ```

   **示例**：
   ```env
   VITE_LOGO_URL=https://abcdefgh.supabase.co/storage/v1/object/public/public/logo.png
   ```

3. **在 Vercel 中添加环境变量**

   - 访问项目 **Settings** → **Environment Variables**
   - 添加变量：
     - **Name**: `VITE_LOGO_URL`
     - **Value**: （粘贴从 Supabase 复制的 URL）
     - **Environment**: `Production`, `Preview`, `Development`（全选）

4. **验证 Logo 显示**

   - 本地测试：`npm run dev`
   - 检查导航栏和 footer 的 logo 是否正确显示

---

## ✅ 配置检查清单

### Vercel 配置
- [ ] ✅ Vercel 项目已连接到 Git 仓库
- [ ] ✅ 自动部署已启用
- [ ] ✅ 推送到 GitHub 后自动部署成功

### 域名配置
- [ ] ✅ 主域名 `ans-scm.com` 已添加到 Vercel
- [ ] ✅ DNS 记录已配置（CNAME 或 A 记录）
- [ ] ✅ DNS 验证通过
- [ ] ✅ SSL 证书自动生成
- [ ] ✅ `https://ans-scm.com` 可以访问

### 子域名配置
- [ ] ✅ `admin.ans-scm.com` 已添加到 Vercel
- [ ] ✅ `admin` CNAME 记录已配置
- [ ] ✅ `wms.ans-scm.com` 已添加到 Vercel
- [ ] ✅ `wms` CNAME 记录已配置
- [ ] ✅ 两个子域名都可以访问

### Logo 配置
- [ ] ✅ 从 Supabase 获取了 logo URL
- [ ] ✅ `.env.local` 中配置了 `VITE_LOGO_URL`
- [ ] ✅ Vercel 环境变量中配置了 `VITE_LOGO_URL`
- [ ] ✅ Logo 在网站上正确显示

---

## 📁 完整 DNS 配置示例

在你的域名注册商配置以下记录：

```
# 主域名
类型: CNAME
名称: @（或留空）
值: cname.vercel-dns.com

# www 子域名（可选）
类型: CNAME
名称: www
值: cname.vercel-dns.com

# admin 子域名（台账系统）
类型: CNAME
名称: admin
值: cname.vercel-dns.com

# wms 子域名（WMS 系统）
类型: CNAME
名称: wms
值: cname.vercel-dns.com
```

⚠️ **重要**：请按照 Vercel 控制台中显示的具体 DNS 值配置，可能因账户不同而不同。

---

## 🔧 环境变量完整列表

### 本地开发（.env.local）

```env
# Supabase 配置
VITE_SUPABASE_URL=你的supabase_url
VITE_SUPABASE_ANON_KEY=你的supabase_anon_key
SUPABASE_PASSWORD=fkMIIsq4Xw46YLne

# Logo URL
VITE_LOGO_URL=https://你的项目ID.supabase.co/storage/v1/object/public/public/logo.png
```

### Vercel 环境变量

在 Vercel 控制台 **Settings** → **Environment Variables** 中添加：

- `VITE_SUPABASE_URL`
- `VITE_SUPABASE_ANON_KEY`
- `VITE_LOGO_URL`

确保选择所有环境：Production, Preview, Development

---

## 🚀 部署和测试

1. **本地测试**
   ```bash
   npm install
   npm run dev
   ```
   访问 http://localhost:3000 验证所有功能

2. **推送到 GitHub**
   ```bash
   git add .
   git commit -m "feat: 完成所有配置"
   git push
   ```

3. **验证 Vercel 自动部署**
   - 在 Vercel Dashboard 查看部署状态
   - 部署成功后访问你的域名

---

## 📚 相关文档

- `VERCEL_CONFIG.md` - Vercel 详细配置
- `SUPABASE_LOGO.md` - Logo 配置详细说明
- `README.md` - 项目说明
- `部署指南.md` - 部署指南

---

## 🆘 问题排查

### DNS 验证失败
- 确认 DNS 记录类型和值正确
- 等待 DNS 传播（最多 72 小时）
- 使用 `nslookup` 检查 DNS 解析

### Logo 不显示
- 确认 Supabase Storage bucket 权限为 Public
- 检查 URL 是否可访问（在浏览器打开）
- 确认环境变量在 Vercel 中正确配置

### 子域名无法访问
- 确认在 Vercel 中已添加子域名
- 确认 DNS CNAME 记录已配置
- 确认子域名指向正确的 Vercel 项目

---

**配置完成后，你的网站就可以正常访问了！** 🎉
