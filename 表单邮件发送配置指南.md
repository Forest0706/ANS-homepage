# 📧 表单邮件发送配置指南

## 📋 当前实现

表单已配置为使用 **mailto** 链接发送邮件到 `info@ans-scm.com`。

### 工作方式：
- 用户提交表单时，会打开系统的默认邮件客户端（Outlook、Mail、Gmail 等）
- 邮件主题和内容已自动填写
- 用户只需点击"发送"即可

---

## ⚠️ 临时方案的限制

mailto 方案的局限性：
- 依赖用户的邮件客户端配置
- 在移动设备上体验可能不佳
- 无法自动发送，需要用户手动点击发送

---

## 🚀 推荐方案：使用 Resend API（专业方案）

### Resend 的优势：
- ✅ 自动发送邮件，无需用户操作
- ✅ 免费额度：每月 3,000 封邮件
- ✅ 设置简单，5 分钟即可配置
- ✅ 可靠性和送达率高

---

## 📝 配置步骤（使用 Resend）

### 步骤 1：注册 Resend 账号

1. 访问：https://resend.com
2. 注册账号（免费）
3. 验证邮箱

### 步骤 2：获取 API Key

1. 登录 Resend Dashboard
2. 进入 **API Keys** 页面
3. 点击 **Create API Key**
4. 复制 API Key（只显示一次，请妥善保管）

### 步骤 3：验证域名（可选但推荐）

1. 在 Resend 中添加你的域名 `ans-scm.com`
2. 按照提示添加 DNS 记录
3. 等待验证通过

### 步骤 4：配置 Vercel 环境变量

1. 访问 Vercel 项目设置
2. **Settings** → **Environment Variables**
3. 添加变量：
   ```
   Name: RESEND_API_KEY
   Value: （粘贴从 Resend 复制的 API Key）
   Environment: Production, Preview, Development（全选）
   ```
4. 点击 **Save**

### 步骤 5：更新 API 文件

需要修改 `api/send-email.js`，使用 Resend API：

```javascript
// 安装 Resend（需要在 package.json 中添加依赖）
// npm install resend

import { Resend } from 'resend';

const resend = new Resend(process.env.RESEND_API_KEY);

export default async function handler(req, res) {
  if (req.method !== 'POST') {
    return res.status(405).json({ error: 'Method not allowed' });
  }

  try {
    const { name, email, phone, company, message, type } = req.body;

    if (!name || !email || !message) {
      return res.status(400).json({ error: 'Missing required fields' });
    }

    const subject = type === 'recruit' 
      ? `[採用応募] ${name}様からの応募`
      : `[お問い合わせ] ${name}様からの問い合わせ`;
    
    const emailBody = `
${type === 'recruit' ? '【採用応募】' : '【お問い合わせ】'}

お名前: ${name}
メールアドレス: ${email}
電話番号: ${phone || '未入力'}
会社名: ${company || '未入力'}
${type === 'recruit' ? '応募ポジション' : 'お問い合わせ内容'}: ${message}

---
このメールは ans-scm.com のフォームから送信されました。
`;

    // 使用 Resend 发送邮件
    const { data, error } = await resend.emails.send({
      from: 'noreply@ans-scm.com', // 或 'ANS <noreply@ans-scm.com>'
      to: 'info@ans-scm.com',
      replyTo: email,
      subject: subject,
      text: emailBody,
    });

    if (error) {
      console.error('Resend error:', error);
      return res.status(500).json({ error: 'Failed to send email' });
    }

    return res.status(200).json({ 
      success: true,
      message: 'Email sent successfully'
    });

  } catch (error) {
    console.error('Error sending email:', error);
    return res.status(500).json({ 
      error: 'Failed to send email',
      message: error.message 
    });
  }
}
```

### 步骤 6：更新前端表单提交

需要修改 `src/App.jsx` 中的表单提交逻辑，改为调用 API：

```javascript
const response = await fetch('/api/send-email', {
  method: 'POST',
  headers: {
    'Content-Type': 'application/json',
  },
  body: JSON.stringify({
    ...formData,
    type: modalType
  })
});

const data = await response.json();

if (!response.ok) {
  throw new Error(data.error || 'Failed to send email');
}
```

---

## 🔄 其他邮件服务选项

### 1. SendGrid
- 免费额度：每月 100 封邮件
- 设置稍微复杂一些

### 2. Mailgun
- 免费额度：每月 5,000 封邮件（前 3 个月）
- 适合高流量网站

### 3. AWS SES
- 非常便宜，但设置复杂
- 适合已有 AWS 账户的用户

---

## ✅ 当前状态

- ✅ 表单已配置 mailto 链接（临时方案）
- ✅ 邮件内容格式化正确
- ⏳ 等待配置 Resend API（推荐升级）

---

## 📞 测试

### 测试当前 mailto 方案：

1. 访问网站
2. 点击"お問い合わせ"或"応募する"
3. 填写表单并提交
4. 应该会打开邮件客户端
5. 检查邮件内容是否正确
6. 发送到 `info@ans-scm.com`

---

## 🆘 问题排查

### 问题：mailto 链接不工作

**可能原因**：
- 浏览器阻止了弹窗
- 系统没有配置邮件客户端

**解决方法**：
- 在浏览器设置中允许弹窗
- 安装并配置邮件客户端

---

**当前使用 mailto 方案，可以立即使用。如需自动发送功能，请按照上述步骤配置 Resend API。**
