# 🔧 Git 设置指南

## 第一次设置 Git 仓库

### 1. 检查 Git 状态

```bash
git status
```

### 2. 添加所有文件到 Git

```bash
git add .
```

### 3. 创建首次提交

```bash
git commit -m "feat: 初始版本 - ANS官网"
```

### 4. 连接到远程仓库

#### 选项 A：使用现有的 GitHub/GitLab 仓库

```bash
# 添加远程仓库
git remote add origin <你的仓库地址>

# 例如：
# git remote add origin https://github.com/yourusername/ans-homepage.git
# 或
# git remote add origin git@github.com:yourusername/ans-homepage.git
```

#### 选项 B：创建新的 GitHub 仓库

1. 访问 https://github.com 登录
2. 点击右上角 "+" → "New repository"
3. 填写仓库名称：`ans-homepage`
4. 选择 Public 或 Private
5. **不要**勾选 "Initialize with README"（我们已经有了）
6. 点击 "Create repository"
7. 复制仓库地址，然后运行：

```bash
git remote add origin <复制的仓库地址>
```

### 5. 设置主分支并推送

```bash
# 设置主分支为 main
git branch -M main

# 推送到远程仓库
git push -u origin main
```

### 6. 验证推送成功

访问你的 GitHub/GitLab 仓库，应该能看到所有文件。

---

## 🔄 日常开发工作流

### 查看更改

```bash
git status
```

### 添加文件

```bash
# 添加所有更改
git add .

# 添加特定文件
git add src/App.jsx
```

### 提交更改

```bash
git commit -m "你的提交信息"

# 提交信息建议格式：
# feat: 添加新功能
# fix: 修复bug
# style: 样式调整
# docs: 文档更新
# refactor: 代码重构
```

### 推送到远程

```bash
git push
```

### 拉取更新

```bash
git pull
```

---

## 🌿 分支管理

### 创建新分支

```bash
git checkout -b feature/新功能名称
```

### 切换分支

```bash
git checkout main
```

### 合并分支

```bash
# 切换到主分支
git checkout main

# 合并功能分支
git merge feature/新功能名称

# 删除已合并的分支
git branch -d feature/新功能名称
```

---

## 🔍 常用命令

```bash
# 查看提交历史
git log

# 查看文件更改
git diff

# 查看远程仓库
git remote -v

# 撤销未提交的更改
git restore <文件名>

# 撤销已暂存的更改
git restore --staged <文件名>
```

---

## ⚠️ 注意事项

1. **不要提交敏感信息**
   - `.env` 文件已在 `.gitignore` 中
   - 密码、API密钥等不要硬编码在代码中

2. **定期提交**
   - 不要累积太多更改才提交
   - 每次提交应该是一个完整的小改动

3. **提交信息要清晰**
   - 使用有意义的提交信息
   - 说明做了什么，为什么做

4. **推送前先拉取**
   - 如果团队协作，推送前先 `git pull`
   - 避免冲突

---

## 🐛 问题排查

### 问题：推送被拒绝

```bash
# 先拉取远程更改
git pull origin main --rebase

# 然后推送
git push
```

### 问题：有未跟踪的文件

```bash
# 查看所有文件（包括未跟踪的）
git status

# 添加特定文件
git add <文件名>

# 或添加所有文件
git add .
```

### 问题：撤销最后一次提交

```bash
# 保留更改，只撤销提交
git reset --soft HEAD~1

# 完全删除更改（谨慎使用）
git reset --hard HEAD~1
```

---

## 📚 相关资源

- [Git 官方文档](https://git-scm.com/doc)
- [GitHub 指南](https://guides.github.com)
- [Git 命令速查表](https://education.github.com/git-cheat-sheet-education.pdf)
