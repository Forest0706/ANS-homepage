# 🚀 ANS 官网部署指南

## 📋 前置准备

### 1. 安装 Node.js
确保已安装 Node.js 18+ 版本：
```bash
node --version
```

### 2. 安装依赖
```bash
npm install
```

---

## 🔧 Git 设置

### 初始化并提交代码

```bash
# 1. 初始化 Git（已完成）
git init

# 2. 添加所有文件
git add .

# 3. 创建首次提交
git commit -m "feat: 初始版本 - ANS官网"

# 4. 连接到 GitHub/GitLab 仓库
# 如果还没有远程仓库，先到 GitHub/GitLab 创建一个
git remote add origin <你的仓库地址>
# 例如：git remote add origin https://github.com/yourusername/ans-homepage.git

# 5. 推送到远程仓库
git branch -M main
git push -u origin main
```

### 常用 Git 命令

```bash
# 查看状态
git status

# 查看远程仓库
git remote -v

# 添加文件
git add .

# 提交更改
git commit -m "你的提交信息"

# 推送到远程
git push

# 拉取更新
git pull
```

---

## 🌐 Vercel 部署

### 方法一：通过 Vercel CLI（推荐）

1. **安装 Vercel CLI**
```bash
npm i -g vercel
```

2. **登录 Vercel**
```bash
vercel login
```
浏览器会打开，登录你的 Vercel 账号。

3. **部署项目**
```bash
# 首次部署（预览环境）
vercel

# 部署到生产环境
vercel --prod
```

4. **配置环境变量（如需要）**
```bash
# 在 Vercel 控制台设置，或使用 CLI
vercel env add VITE_SUPABASE_URL
```

### 方法二：通过 GitHub 自动部署（推荐生产环境）

1. **将代码推送到 GitHub**
```bash
git push origin main
```

2. **在 Vercel 网站**
   - 访问 https://vercel.com
   - 点击 "Import Project"
   - 选择你的 GitHub 仓库
   - Vercel 会自动检测 Vite 项目
   - 点击 "Deploy"

3. **自动部署**
   - 以后每次 `git push` 都会自动触发部署
   - 每个 PR 都会创建预览部署

### Vercel 配置说明

项目已包含 `vercel.json`，配置了：
- 构建命令：自动使用 `vite build`
- 输出目录：`dist`
- SPA 路由：所有路由都指向 `index.html`

---

## 🗄️ Supabase 配置（如需要）

### 1. 创建 Supabase 项目

1. 访问 https://supabase.com
2. 创建新项目
3. 获取项目 URL 和 API Key

### 2. 配置环境变量

**本地开发** - 创建 `.env.local`：
```env
VITE_SUPABASE_URL=你的supabase项目URL
VITE_SUPABASE_ANON_KEY=你的anon key
```

⚠️ **重要**：`.env.local` 已在 `.gitignore` 中，不会被提交到 Git。

**Vercel 部署** - 在 Vercel 控制台：
1. 进入项目设置
2. 找到 "Environment Variables"
3. 添加环境变量：
   - `VITE_SUPABASE_URL`
   - `VITE_SUPABASE_ANON_KEY`

### 3. 在代码中使用

```javascript
const supabaseUrl = import.meta.env.VITE_SUPABASE_URL
const supabaseKey = import.meta.env.VITE_SUPABASE_ANON_KEY
```

---

## 📝 开发工作流

### 本地开发

```bash
# 启动开发服务器
npm run dev

# 浏览器会自动打开 http://localhost:3000
```

### 构建测试

```bash
# 构建生产版本
npm run build

# 预览构建结果
npm run preview
```

---

## 🔍 检查清单

部署前请确认：

- [ ] ✅ 代码已推送到 Git 远程仓库
- [ ] ✅ `package.json` 中的依赖正确
- [ ] ✅ `.gitignore` 已配置（排除 node_modules 等）
- [ ] ✅ `vercel.json` 配置正确
- [ ] ✅ 环境变量已设置（如需要）
- [ ] ✅ 测试过本地构建：`npm run build`
- [ ] ✅ 域名已配置（如需要）

---

## 🐛 常见问题

### 1. 构建失败

**问题**：Vercel 构建时找不到依赖
**解决**：确保 `package.json` 存在且依赖正确

### 2. 路由404

**问题**：刷新页面后404
**解决**：`vercel.json` 已配置 SPA 路由，如仍有问题检查路由配置

### 3. 环境变量未生效

**问题**：生产环境环境变量不生效
**解决**：
- 确保变量名以 `VITE_` 开头
- 在 Vercel 控制台重新设置环境变量
- 重新部署项目

### 4. Supabase 连接失败

**问题**：Supabase 请求失败
**解决**：
- 检查环境变量是否正确
- 检查 Supabase 项目的 API 设置
- 确认网络请求未被 CORS 阻止

---

## 📞 支持

如有问题，请联系：
- **邮箱**: l.li@ans-scm.com
- **电话**: 045-349-3730

---

## 🔗 相关链接

- [Vercel 文档](https://vercel.com/docs)
- [Vite 文档](https://vitejs.dev)
- [React 文档](https://react.dev)
- [Supabase 文档](https://supabase.com/docs)
