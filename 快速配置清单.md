# ⚡ 快速配置清单

## 📋 你的4个需求

### ✅ 1. Vercel 连接 Git（已完成代码，需要手动操作）

**操作步骤**：
1. 访问：https://vercel.com/forestlis-projects/ans_homepage/settings/git
2. 点击 **Connect Git Repository**
3. 选择 **GitHub** 并授权
4. 选择仓库：`Forest0706/ANS-homepage`
5. 点击 **Import**

**完成标志**：推送到 GitHub 后，Vercel 会自动部署。

---

### ✅ 2. 配置主域名 ans-scm.com（需要手动操作）

**操作步骤**：
1. 访问：https://vercel.com/forestlis-projects/ans_homepage/settings/domains
2. 点击 **Add Domain**
3. 输入：`ans-scm.com`
4. 点击 **Add**
5. 按照 Vercel 显示的 DNS 记录配置
6. 在你的域名注册商配置 DNS：
   ```
   类型: CNAME
   名称: @（或留空）
   值: cname.vercel-dns.com
   ```

**完成标志**：DNS 验证通过后，`https://ans-scm.com` 可以访问。

---

### ✅ 3. 配置子域名（需要手动操作）

#### admin.ans-scm.com（台账系统）

**如果台账系统是独立的 Vercel 项目**：
1. 创建新 Vercel 项目：`ans-admin`
2. 连接 Git 仓库
3. 在 **Settings** → **Domains** 添加：`admin.ans-scm.com`
4. 配置 DNS：
   ```
   类型: CNAME
   名称: admin
   值: cname.vercel-dns.com
   ```

#### wms.ans-scm.com（WMS 系统）

**如果 WMS 系统是独立的 Vercel 项目**：
1. 创建新 Vercel 项目：`ans-wms`
2. 连接 Git 仓库
3. 在 **Settings** → **Domains** 添加：`wms.ans-scm.com`
4. 配置 DNS：
   ```
   类型: CNAME
   名称: wms
   值: cname.vercel-dns.com
   ```

**完成标志**：两个子域名都可以访问。

---

### ✅ 4. 配置 Logo（需要获取 Supabase URL）

**操作步骤**：

1. **获取 Logo URL**
   - 登录 Supabase Dashboard
   - **Storage** → **Buckets** → 找到 logo 文件
   - 点击 logo 文件，复制 **Public URL**
   - URL 类似：`https://xxxxx.supabase.co/storage/v1/object/public/bucket名称/logo.png`

2. **更新本地环境变量**
   ```bash
   # 编辑 .env.local 文件
   # 添加或更新：
   VITE_LOGO_URL=粘贴从Supabase复制的URL
   ```

3. **在 Vercel 中添加环境变量**
   - 访问：https://vercel.com/forestlis-projects/ans_homepage/settings/environment-variables
   - 添加：
     - **Name**: `VITE_LOGO_URL`
     - **Value**: （粘贴 Supabase URL）
     - **Environment**: 全选（Production, Preview, Development）
   - 点击 **Save**

4. **重新部署**
   - 如果有更改，推送到 GitHub 触发自动部署
   - 或手动触发部署

**完成标志**：网站导航栏和 footer 显示你的 logo。

---

## 📝 现在需要做的事情

### 立即执行（按顺序）：

1. **✅ 代码已完成** - 所有代码更改已就绪
2. **🔧 配置 Vercel Git** - 按照上面的步骤1
3. **🌐 配置域名** - 按照上面的步骤2和3
4. **🖼️ 配置 Logo** - 按照上面的步骤4

### 提交代码到 Git：

```bash
git add .
git commit -m "feat: 添加 Vercel 配置、域名支持、Supabase Logo 集成"
git push
```

---

## 📚 详细文档

- **完整配置指南.md** - 所有配置的详细说明
- **VERCEL_CONFIG.md** - Vercel 配置详细说明
- **SUPABASE_LOGO.md** - Logo 配置详细说明

---

## ✅ 配置检查清单

- [ ] ✅ Vercel 项目已连接到 Git 仓库
- [ ] ✅ 主域名 `ans-scm.com` 已添加并配置 DNS
- [ ] ✅ 子域名 `admin.ans-scm.com` 已配置
- [ ] ✅ 子域名 `wms.ans-scm.com` 已配置
- [ ] ✅ 从 Supabase 获取了 logo URL
- [ ] ✅ `.env.local` 中配置了 `VITE_LOGO_URL`
- [ ] ✅ Vercel 环境变量中配置了 `VITE_LOGO_URL`
- [ ] ✅ 所有更改已提交并推送到 GitHub

---

**需要帮助？查看 `完整配置指南.md` 获取详细步骤！** 🚀
